spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sandbox?useSSL=false&serverTimezone=UTC
    username: root
    password: 4370
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  mail:
    host: smtp.naver.com
    port: 465
    username:
    password:
    properties:
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true
      mail.smtp.ssl.trust: smtp.naver.com

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
